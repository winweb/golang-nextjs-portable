{
  "name": "golang-nextjs-portable",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "rm -rf .next && next build && next export -o dist",
    "export-win": "rmdir /s /q .next && next build && next export -o dist",
    "build-export": "next build && next export -o dist"
  },
  "dependencies": {
    "@material-ui/core": "^4.11.4",
    "@material-ui/data-grid": "^4.0.0-alpha.31",
    "next": "10.1.3",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "sass": "^1.34.0",
    "swr": "0.5.5"
  },
  "devDependencies": {
    "@types/react": "17.0.3",
    "typescript": "4.2.4"
  }
}
